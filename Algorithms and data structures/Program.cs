/*Use this file to run the code, copy paste the code from the other files
The property of the other .cs files are build action none, so they dont compile, they are just files to copy paste from into here
*/


//recursively (top down) add all numbers inside an array and modify it in place
/*int add(int[] array)
{
    int[] reducingArray = new int[array.Length-1];
    Array.Copy(array, 1, reducingArray, 0, array.Length - 1);
    if (reducingArray.Length > 0) 
        return array[0] + add(reducingArray);
    else
        return 
}

Console.WriteLine(add(new int[] { 1,2,3,4,5 }));*/


